export enum CursorType {
  DATE = 'DATE',
  STRING = 'STRING',
  NUMBER = 'NUMBER',
}

export enum QueryCursor {
  DATE = 'DATE',
  ALPHA = 'ALPHA',
}

export enum QueryOrder {
  ASC = 'ASC',
  DESC = 'DESC',
}
